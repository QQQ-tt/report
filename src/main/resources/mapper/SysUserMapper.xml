<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qtx.report.mapper.SysUserMapper">

    <select id="selectPageNew" resultType="com.qtx.report.pojo.vo.SysUserVo">
        select u.id, u.name, u.card, u.status, sr.role_name
        from (select * from sys_user where delete_flag = '0') u
                 left join sys_user_role sur on u.card = sur.user_card
                 left join sys_role sr on sur.role_id = sr.role_id
            ${ew.customSqlSegment}
    </select>
</mapper>
